export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const body=typeof req.body==='string'?JSON.parse(req.body):req.body;const required=['fullName','email','phone','business','interest','message'];if(required.some(k=>!body?.[k]))return res.status(400).json({error:'Missing required fields'});console.log('CONTACT_ENQUIRY',body);return res.status(200).json({ok:true,message:'Enquiry received'});}