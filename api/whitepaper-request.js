export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const body=typeof req.body==='string'?JSON.parse(req.body):req.body;if(!body?.name||!body?.email||!body?.phone)return res.status(400).json({error:'Missing required fields'});console.log('WHITEPAPER_REQUEST',body);return res.status(200).json({ok:true,message:'Request received'});}